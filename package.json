{
  "name": "family-meal-planner",
  "version": "1.0.0",
  "private": true,
  "description": "Static family meal planner + Trader Joe's shopping list",
  "scripts": {
    "start": "serve -s . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
